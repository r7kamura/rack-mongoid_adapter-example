# Example of Rack::MongoidAdapter
Try the following script:

```sh
git clone git@github.com:r7kamura/rack-mongoid_adapter-example.git
cd rack-mongoid_adapter-example
heroku create
heroku addons:add mongolab
git push heroku master
```
